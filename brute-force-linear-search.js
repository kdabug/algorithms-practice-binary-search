//  search through the array recursively for the element
//  if the element is not found, return -1
//  if the element is found, return the index at which it was found
function bruteForceLinearSearch(arr, element) {}
